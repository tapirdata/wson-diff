debug = require('debug') 'wson-diff:target'

class Target

  get: (outSteps) ->

  budge: (outSteps, key) ->

  assign: (key, value) ->

  unset: (key) ->

  replace: (key, values) ->

  delete: (key, len) ->

  insert: (key, values) ->

  move: (srcKey, dstKey, len, reverse) ->


module.exports = Target
