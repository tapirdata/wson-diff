debug = require('debug') 'wson-diff:target'

class Target

  getCurrent: ->  

  budge: (outSteps, key) -> 

  assign: (key, values) -> 

  delete: (key, len) -> 

  insert: (key, values) -> 

  move: (srcKey, dstKey, len) ->


module.exports = Target
